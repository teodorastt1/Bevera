@model Bevera.Models.Catalog.Product

@{
    ViewData["Title"] = Model.Name;

    // ако при теб StockQty го има – супер.
    // ако дава грешка тук, значи полето се казва различно.
    var inStock = Model.StockQty > 0;

    // Без Images, без MainImagePath -> само placeholder, който винаги съществува
    var imageSrc = Url.Content("~/images/image-1.jpg");
}

<div class="container py-4">

    <a class="btn btn-link px-0 mb-3" asp-controller="Client" asp-action="Index">
        ← Назад
    </a>

    <div class="row g-4 align-items-start">

        <div class="col-lg-5">
            <div class="border rounded-4 p-3 bg-white shadow-sm">
                <img src="@imageSrc" alt="@Model.Name"
                     class="img-fluid rounded-3"
                     style="width:100%; max-height:420px; object-fit:contain;" />
            </div>
        </div>

        <div class="col-lg-7">
            <div class="border rounded-4 p-4 bg-white shadow-sm">
                <h2 class="mb-2">@Model.Name</h2>

                <div class="fs-3 fw-bold mb-3">
                    @Model.Price.ToString("0.00") лв.
                </div>

                <div class="mb-3">
                    @if (inStock)
                    {
                        <span class="badge bg-success">Налично</span>
                    }
                    else
                    {
                        <span class="badge bg-danger">Няма наличност</span>
                    }

                    <span class="ms-2 text-muted">Наличност: @Model.StockQty</span>
                </div>

                @* Ако имаш Description – ще го покажем. Ако ти дава грешка, кажи и ще го вържем към твоето поле. *@
                @if (!string.IsNullOrWhiteSpace(Model.Description))
                {
                    <div class="mt-3">
                        <h6 class="text-uppercase text-muted fw-bold">Описание</h6>
                        <p class="mb-0">@Model.Description</p>
                    </div>
                }

                <hr class="my-4" />

                <div class="text-muted">
                    (Следваща стъпка: тук добавяме бутони “Добави в количка” и “Любими”, когато си готова с Cart/Favourites.)
                </div>
            </div>
        </div>

    </div>
</div>
